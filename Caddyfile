mcp.belleq.app {
	# Caddy auto-fetches a Let's Encrypt cert for mcp.belleq.app.
	# The only requirement: this EC2 must be reachable on port 443 from the
	# internet (add an inbound rule in the EC2 security group if needed).
	reverse_proxy master:9000
}
